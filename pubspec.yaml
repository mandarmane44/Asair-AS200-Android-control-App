name: asair_control
description: ASAir Hardware Controller for Android.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  usb_serial: ^0.3.0

flutter:
  uses-material-design: true
